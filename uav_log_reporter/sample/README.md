# Sample 안내

실제 `.BIN` 로그는 용량이 크고 민감 데이터가 포함될 수 있어 저장소에 포함하지 않았습니다.

아래 형식으로 로그를 준비해 실행하세요.

```bash
python main.py --bin "logs/Flight10.BIN" --flight 10 --day 5 --date "26.04.23"
```

파이프라인 동작만 빠르게 확인하려면 `--mock` 옵션을 사용하세요.
